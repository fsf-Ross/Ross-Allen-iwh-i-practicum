//- ** This is a sample of a pug template and how it uses the data passed to it from index.js.

doctype html
html 
     head 
         title= `${title}`
         meta(name="viewport" content="width=device-width, initial-scale=1")
         link(rel="stylesheet", href="/css/style.css")
     body    
         h1 Map Territories
         .cards
             each map_territories in data
                 .card 
                     h2.card__name #{map_territories.properties.territory_name}
                     p.card__status #{map_territories.properties.status}